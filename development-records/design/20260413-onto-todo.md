---
as_of: 2026-04-13T16:25:00+09:00
supersedes: null
status: active
revision: v1.2
functional_area: onto-todo-canonical-seat
purpose: onto 축 A/B/C/D work item canonical output (M-06 산출 저장소). M-04 Phase A 에서 schema + seat 만 확정, item 은 M-06 에서 채움.
schema_version: v1.1
lexicon_version_at_creation: "0.6.1"
activity_enum_ref: "authority/core-lexicon.yaml#activity_enum.v0.6.1"
axis_enum_ref: "authority/core-lexicon.yaml#axis_enum.v0.6.1"
canonicality_enum_ref: "development-records/plan/20260413-m03-decisions.md#DR-M03-03"
disposition_enum_ref: "development-records/plan/20260413-m03-decisions.md#DR-M03-02"
lifecycle_status_enum_ref: "development-records/design/20260413-onto-todo.md#1-2-enum-값-목록-본-파일-시점-snapshot"  # v1.1: self-ref (본 파일 §1.2 lifecycle_status enum 값 정의가 canonical). 이전 meta v5.1 anchor 는 broken 이었음 (C-01 해소).
id_scheme: "W-{axis}-{nn:02d}. axis ∈ {A,B,C,D}. nn 은 axis 내 sequential (01..99). 축당 용량 99 — 포화 시 schema_version major bump (regex `\\d{2,3}` 확장, C-03 연계). axis 변경 시 deprecated_aliases + previous_id 로 continuity (Migration Contract)."
append_revise_rule: "append + inline revise 허용. 삭제 금지 — status 를 `deprecated` 또는 `archived` 로 전환. 6개월 후 archived 는 별도 expansion-migration 파일로 이관. (상세: §3)"
source_refs:
  consolidated: "development-records/plan/20260413-backlog-consolidated.md v3.2 (commit 698df41)"
  m03_decisions: "development-records/plan/20260413-m03-decisions.md v1.3"
  m04_decisions: "development-records/plan/20260413-m04-decisions.md v1.1"  # CC-03 추가
  deferred_ledger: "development-records/plan/20260413-deferred-ledger.md v1.6 (M-04-A v1.1 완료 후)"
  onto_direction: "development-records/design/20260413-onto-direction.md (§1 정본, as_of=2026-04-13)"
  meta: "development-records/design/20260413-onto-todo-meta.md v5.1"
  core_lexicon: "authority/core-lexicon.yaml v0.6.1"  # CC-03 추가
item_count: 0  # M-06 에서 채움
by_axis_count: { A: 0, B: 0, C: 0, D: 0 }
decision_records_applied:
  - id: DR-M04-01
    status: applied
    note: "activity_enum + axis_enum canonical seat 확정 (authority/core-lexicon.yaml v0.6.1)"
  - id: DR-M04-02
    status: applied
    note: "activity_enum_ref + axis_enum_ref frontmatter 필드 의무화. 본 파일 frontmatter 에 반영. v1.1 에서 anchor unresolvable reject 규약 추가."
authority_stance: non-authoritative-design-surface
canonicality: canonical-advancing  # work item canonical output seat 자체가 §1.5 축 build-out 직접 advance
review_session_refs:
  - id: ".onto/review/20260413-a9e93dd7"
    executor: "agent-teams + claude"
    verdict: "CONDITIONAL → resolved by v1.1 patch"
  - id: ".onto/review/20260413-de95c971"
    executor: "subagent + codex (synthesize in-process deliberation)"
    verdict: "PASS-with-residuals → Immediate 1/2/4 + Rec 2/3 applied via v1.2 patch"
---

# onto Work Item Canonical Output (2026-04-13)

본 문서는 onto 축 A/B/C/D work item 의 **canonical output seat** 이다. M-04 Phase A 에서 schema + seat 구조만 확정하고, 실제 work item 은 **M-06 (축별 Work Item 초안)** 에서 작성된다.

## 0. 위상·경계

- **canonicality**: canonical-advancing (축 A/B/C/D 구현의 "무엇을 할지" 를 canonical 로 정렬하는 output seat).
- **authority_stance**: non-authoritative-design-surface. 본 파일은 design record 이며 rank 1 authority 아님. authority SSOT 는 `authority/core-lexicon.yaml` (enum refs) 및 `development-records/design/20260413-onto-direction.md` (§1 정본).
- **소비 제약**: 본 파일의 W-ID 인용은 **M-06 이후** 에만 허용. M-04 Phase A 단계의 W-ID 는 schema 검증용 dry-run 전용.

## 1. Schema 명세 (v1, 17 필드)

v5.1 meta 표의 필드 집합을 기준으로 v1 초판을 확정한다. 필드 수는 meta v5.1 원문(활동 포함 17개) 기준이며, 일부 초기 문건에서 "15 필드" 라고 표기된 것은 기점 버전 차이(v4=11, v5=16, v5.1=17)로 인한 표기 혼선 — 본 schema 가 canonical.

### 1.1 필드 테이블

| # | 필드 (canonical) | Korean | Type | Required | Constraint | Enum / Source ref | Relation |
|---|---|---|---|---|---|---|---|
| 1 | `id` | 식별자 | string | **yes** | `^W-[ABCD]-\d{2}$`. 축당 용량 99 — 포화 시 schema major bump (§1.5) | axis_enum_ref | — |
| 2 | `title` | 제목 | string | **yes** | ≤80 chars, 한 줄 요약 | — | — |
| 3 | `files` | 수정 파일 | list<string> | no | repo-relative path. **glob 허용** (`**`, `*`). 각 glob 의 prefix (first `**`/`*` 이전) 는 `ownership_boundary` 에 포함되어야 함 (§1.3 불변식) | — | **cluster 판정 근거** (M-07) |
| 4 | `depends_on` | 의존 | list<string> | no | 각 원소 `^(W-[ABCD]-\d{2}|BL-\d{3}|DL-\d{3})$`. §1 정본 anchor / DR-NN / URL 은 본 필드 **불허** — `provenance` 필드 전용 | activity_enum_ref 와 독립 | DAG (§1.3 acyclic). M-05 에서 값 채움 |
| 5 | `verification_consumer` | 검증 주체 | string | **yes** | enum | {principal, runtime, test, reviewer, toolchain} | — |
| 6 | `verification_method` | 검증 방법 | string | **yes** | free-form (method 유형 명시) | 예: "unit test", "9-lens review", "Principal 읽기", "gh pr checks" | — |
| 7 | `evidence_seat` | 증거 seat | string | **yes** | repo-relative path 또는 URL pattern | 예: `.onto/review/{session}/`, `src/**/*.test.ts` | — |
| 8 | `blockers` | 차단 원인 | list<string> | no | 각 원소 free-form + optional id ref | 예: "W-A-02 pending", "Principal 승인 대기" | — |
| 9 | `ownership_boundary` | 수정 주체 경계 | string | **yes** | module path 또는 role 명시 | 예: `src/core-runtime/design/`, `processes/review/`, `role:scope-runtime-owner`, `role:principal` | — |
| 10 | `canonicality_class` | canonicality | string | **yes** | enum | {canonical-advancing, supporting, scaffolding} | DR-M03-03 |
| 11 | `completion_criterion` | 완료 기준 | string | **yes** | observable event (파일·state·metric 관찰 가능) | — | — |
| 12 | `lifecycle_status` | 생애 상태 | string | **yes** | enum. **초기값: `active`** (G-2). 전이 주체: M-08 protocol | {active, deferred, done, deprecated, archived} | M-08 protocol (전이 관리) |
| 13 | `provenance` | 기원 | list<string> | **yes** | 각 원소 `^(BL-\d{3}|DL-\d{3})$` 또는 `§<section>` anchor 또는 URL. §1 정본 anchor / DR-* 는 본 필드에 허용 (depends_on 과 경계) | consolidated / ledger / §1 정본 / m*-decisions | origin trace. 최소 1개 |
| 14 | `activity` | 활동 | string \| list<string> | **yes** | enum (기본 단일, 복수 허용 — interface 성격 work item 에 한함) | activity_enum_ref | — |
| 15 | `deprecated_aliases` | 이전 ID | list<string> | no | 각 원소 `^W-[ABCD]-\d{2}$`. 다수 rename 누적 시 모두 보존 | — | Migration Contract (검색·참조용) |
| 16 | `previous_id` | 직전 ID | string | no | `^W-[ABCD]-\d{2}$`. **직전 1개** (reissue 계보 head). 더 과거 이력은 `deprecated_aliases` 에 누적 | — | Migration Contract (reissue 직전 1개) |
| 17 | `rewrite_trace` | 재작성 이력 | list<object> | no | `{date, change_type, old_id?, new_id?, note?}`. `change_type ∈ {axis_move, id_reissue, id_width_expansion, activity_rename, activity_merge, deps_rewrite, schema_version_bump}` | change_type enum (본 row) | Migration Contract. **의미**: 본 work item 의 ID/axis/activity 자체 변경 이력 + 본 item 의 `deps` 필드 값 재작성 이력을 모두 포함 (단일 log) |

### 1.2 Enum 값 목록 (본 파일 시점 snapshot)

- **activity_enum** (`authority/core-lexicon.yaml#activity_enum.v0.6.1`): `review`, `design`, `reconstruct`, `learn`, `govern`. legacy_aliases: `build → reconstruct (renamed)`, `ask → null (retired)`, `E/D/R/L/G → shortcode`.
- **axis_enum** (`authority/core-lexicon.yaml#axis_enum.v0.6.1`): `A` (entrypoint 구현), `B` (기반 인프라), `C` (자율성 진화), `D` (상시 원칙, lexicon provisional lifecycle).
- **canonicality_class** (DR-M03-03): `canonical-advancing`, `supporting`, `scaffolding`.
- **lifecycle_status**: `active` (진행 중), `deferred` (보류), `done` (완료 후 유지), `deprecated` (폐기 후보, 미삭제), `archived` (폐기 완료, 6개월 후 expansion-migration 파일로 이관).
- **verification_consumer**: `principal`, `runtime`, `test`, `reviewer`, `toolchain` (lint/typecheck 등).

### 1.3 필드 간 관계·불변식

- **id ⇔ axis**: `id` 의 axis token 이 `activity` 와 충돌하지 않아야 한다 — 단, activity-축 관계는 soft (A0=scope-runtime 같은 infra advance 는 `axis=B` + `activity=reconstruct` 조합이 자연스러울 수 있음). validator 경고 수준 (M-07 hard reject 아님).
- **depends_on ⇒ acyclic**: W-ID 간 의존은 DAG. cycle 발견 시 M-07 reject.
- **depends_on 의미 구분** (C-04 해소, v1.1 추가):
  - `W-[ABCD]-NN` 원소 = **실행 DAG 선행 관계** (해당 work item 이 선행되어야 본 work item 이 실행 가능)
  - `BL-NNN` 원소 = **resolves 관계** (본 work item 이 해당 backlog 항목을 해결)
  - `DL-NNN` 원소 = **resolution_stage 도달** (본 work item 의 완료가 해당 deferred item 을 resolved 로 전환)
  - 세 종류는 의미가 다르므로 M-05 dep graph 는 각각 별도 edge 로 모델링.
- **provenance vs depends_on 분리**: §1 정본 anchor / DR-NN / URL 은 **provenance 필드 전용** (origin 증빙). `depends_on` 은 W/BL/DL ID 만 허용 (실행·해결 관계).
- **provenance 필수**: 최소 1개. origin 이 없으면 본 M-03 gap 도출 근거가 없음을 의미 (reject).
- **activity 복수 귀속**: 활동 간 interface 성격인 work item (예: `[review, learn]` — review 실행 중 learning 자동 수집) 만 복수 허용. 기본 단일.
- **lifecycle_status 전이**: `active ↔ deferred` 양방향, `active → done`, `active → deprecated → archived` 일방향. 전이 주체는 M-08 protocol.
- **Migration Contract**: axis 변경 시 `previous_id` + `deprecated_aliases` 기록 필수. rename log 는 `rewrite_trace` 에 `{date, change_type: "axis_move", old_id: "W-A-05", new_id: "W-B-12"}` 형식.
- **Cluster 판정 알고리즘** (C-05 해소, v1.1 추가, M-07 책임):
  - 두 work item A, B 가 **동일 cluster 후보** 인 조건: A.files 와 B.files 의 glob expansion 결과가 1개 이상의 파일을 공유하거나, 한 glob 의 prefix 가 다른 glob 의 expansion 에 포함.
  - Cluster 내 work item 은 순차 실행 필수 (같은 파일 동시 수정 금지).
  - 예외: `files` 가 빈 list 인 work item 은 cluster 판정 대상이 아님 (ownership_boundary 만으로 판정).
- **ownership_boundary ↔ files prefix 불변식** (C-05 해소): 각 work item 의 `files` glob 에서 first `**` 또는 `*` 이전 prefix 는 `ownership_boundary` 에 포함되어야 한다 (role-based boundary 는 예외 — 해당 role 의 책임 범위 내 glob 허용).

### 1.4 valid record 예시 (schema sanity check, 실제 item 아님)

```yaml
id: W-B-01                      # 예시 — M-06 에서 실제 할당
title: scope-runtime framework 추출 (A0)
files:
  - src/core-runtime/scope-runtime/**
  - processes/build.md
depends_on: []                  # A0 는 축 A 전체 선행 — 자신의 의존 없음
verification_consumer: test
verification_method: "type check + unit test + 1 full run end-to-end"
evidence_seat: ".onto/temp/a0-framework-extraction/verification/"
blockers: []
ownership_boundary: "src/core-runtime/scope-runtime/"
canonicality_class: canonical-advancing
completion_criterion: "state-machine 3중 구현 통합 완료 (review 9-state + scope-runtime 15-state + build.md spec 일원화)"
lifecycle_status: active
provenance:
  - DL-016
  - DL-018
activity: reconstruct
deprecated_aliases: []
previous_id: null
rewrite_trace: []
```

### 1.5 schema version 진화 규칙

- **표기법** (G-3 / C-10 해소, v1.1 명시): `schema_version: v{major}.{minor}` (예: v1, v1.1, v2). minor 는 additive 시 증가, major 는 breaking 시 증가. integer-only 는 v1, v2 처럼 minor 0 표기 생략 가능.
- **additive change** (필드 추가, enum 값 추가, constraint 완화): `schema_version` minor bump. enum 값 추가 시 `lexicon_version` 도 minor bump (core-lexicon SSOT 동기화).
- **breaking change** (필드 삭제, enum 값 삭제, 의미 변경, regex 자릿수 축소): `schema_version` major bump. 기존 item 전수 migration (Expansion Protocol Migration Contract). lexicon_version 도 의미 변경 시 major bump (core-lexicon `schema_version_evolution` 규약 준수).
- **ID 공간 확장 trigger** (C-03 해소, v1.1 추가): `axis` 별 `nn:02d` ID 가 99 에 도달하면 `schema_version` major bump + ID regex 를 `^W-[ABCD]-\d{2,3}$` 또는 `\d{3}` 으로 확장. **선제적 확장** 도 옵션 — Principal 판단으로 본 schema v1.x 단계에 미리 `\d{2,3}` 으로 확장 가능 (item 0건 상태에서 확장 비용 최저).
- **archived ID nn 재사용 금지** (Migration Contract 보강): 본 schema 는 W-ID 를 stable 로 가정. archived 로 이관된 ID 의 nn 을 새 work item 에 재할당 금지 — 새 item 은 axis 내 가장 큰 nn+1 로 reissue.
- **trigger**: M-04 Phase A 결과 schema 변경 필요 시 M-04 reopen. M-06 이후 발견 시 M-08 refresh protocol 경유.
- **downstream reader 재파싱 요건**: DL-027 (v2-review E-7) 에서 수렴. schema_version 변경 시 본 파일 frontmatter + consolidated frontmatter + 각 work item 의 schema 준수 재검증.

## 2. Canonical Output 구조 (축별 섹션)

M-06 이 본 섹션을 채운다. **M-04 Phase A 종료 시점엔 빈 구조만 확정**.

### W-A (entrypoint 구현)

**정의**: 다섯 활동(review, design, reconstruct, learn, govern)의 command·process·calling convention 구현.

**의존**: 축 A 의 모든 work item 은 **A0** (scope-runtime framework 추출, **axis=B 에 위치하는 W-B-NN**, activity=reconstruct) 을 `depends_on` 으로 가진다. 즉 A0 자체는 axis=B 의 work item 이며, axis=A 활동 구현이 시작되기 전에 완료되어야 한다. 구체 W-ID 는 M-05 dep graph 에서 명시.

| id | title | activity | canonicality | lifecycle | provenance |
|---|---|---|---|---|---|
| _M-06 에서 채움_ | | | | | |

### W-B (기반 인프라)

**정의**: product·ontology·learning 저장·처리 공통 인프라. scope-runtime, readers, middleware 기반.

**의존 (axis arrow 명료화)**: 축 정의 §1.5 "A→B" 표기는 "A 가 B 에 의존" 의미 — **B 는 A 의 선행**. axis=B 의 work item 은 그 자체 내부 의존만 가지며 (예: A0 framework 가 다른 W-B-NN 의 선행), axis=A 의 work item 을 depends_on 으로 가지지 않는다. **A0 (scope-runtime framework 추출) 의 위치 ownership: axis=B**.

| id | title | activity | canonicality | lifecycle | provenance |
|---|---|---|---|---|---|
| _M-06 에서 채움_ | | | | | |

### W-C (자율성 진화)

**정의**: self-application 메커니즘 + drift 판정 + 큐·승인 흐름 + 수준 0→1→2 엔진.

**의존**: C → {A, B} (자율성은 A·B 최소 1사이클 운영 후 설계 가능).

| id | title | activity | canonicality | lifecycle | provenance |
|---|---|---|---|---|---|
| _M-06 에서 채움_ | | | | | |

### W-D (상시 원칙)

**정의**: lexicon provisional lifecycle 관리 (교차 원칙, 독립 축 아님).

**의존**: D → {A, B, C} 교차. 축 D 선행 확인 (§1 "다음 작업 #2") 은 BL-123.

| id | title | activity | canonicality | lifecycle | provenance |
|---|---|---|---|---|---|
| _M-06 에서 채움_ | | | | | |

## 3. 갱신 규칙 (append + revise)

- **신규 work item 추가**: 본 파일 해당 축 섹션에 행 추가. `id` 는 axis 내 sequential (기존 최대값 + 1, 공백 허용).
- **revise**: 기존 행 값 변경 시 `lifecycle_status` 를 유지하되 `rewrite_trace` 에 변경 이력 추가.
- **삭제 금지**: `lifecycle_status=deprecated` 또는 `archived` 로 전환. 6개월 경과 archived 는 `development-records/design/expansion-migration-{YYYYMMDD}.md` 에 이관.
- **rename**: Migration Contract 경유. `previous_id` + `deprecated_aliases` 기록 필수. `rewrite_trace` 에 `change_type` 명시.

## 4. Phase B 접합점 (sanity check 용)

M-04 Phase A 는 schema + seat 만 확정하고 종료. **Phase B (선택) 는 M-05 dep graph 작성 착수 전** 에 gap × canonical-advancing 7건 중 일부를 샘플로 W-ID 생성하여 schema 수용성 sanity check. ledger DL-031 (v1.6 신설) 가 본 Phase B 실행을 추적.

### 4.1 후보 (현재 2건, M-05 직전 재평가)

- BL-123 (축 D 선행 확인, §1 "다음 작업 #2") → `W-D-01`
- BL-120 (Feature 15 Consumption Feedback Verification, §1.0 기제 측정) → `W-C-01`

축 C/D 만 샘플인 점은 의도적 제한 (ledger DL-031 context 참조). 축 A/B schema 수용성은 **M-06 전수 검증에 위임** — Phase B 의 효용 대비 비용이 낮음.

### 4.2 Exit 경로 (C-02 해소, v1.1 추가)

| 경로 | trigger | 동작 |
|---|---|---|
| **PASS** | 샘플 W-ID 가 schema 17 필드 + 모든 불변식 (§1.3) 을 위반 없이 채워짐 | 본 §2 축 C/D 섹션에 샘플 행 추가 + 본 파일 v1.2 patch (revision_note_v1_2 추가). DL-031 resolved. M-05 진입. |
| **FAIL (schema)** | 샘플 채움 중 schema 결함 발견 (필드 부족, 불변식 위반, enum 미정의) | M-04 reopen → DR-M04-NN 신규 또는 schema major bump. ledger 에 DL-NNN 추가 (resolution_stage=M-04-A). |
| **FAIL (boundary)** | 샘플 자체 정의가 불명확 (gap × canonical-advancing 후보 본질 결함) | M-03 reopen → DR-M03-NN revise. consolidated v3.x 재revision. |

### 4.3 추적 경로

- **선언 seat**: 본 파일 §4 (현재 문서) + ledger DL-031 (`resolution_stage=M-05`).
- **실행 주체**: M-05 startup handoff 에서 명시 — Phase B 를 실행하는 agent/session 이 명시적으로 선언.
- **rationale 기록**: execution-log M-04-A entry `phase_b_skip_rationale` 에 v1.1 시점 skip 사유 등재 (효용 대비 비용 평가).

## 5. 참조

- `development-records/design/20260413-onto-direction.md` §1 (정본)
- `development-records/design/20260413-onto-todo-meta.md` v5.1 (필드 schema + canonical output spec + Expansion Protocol Migration Contract)
- `development-records/plan/20260413-backlog-consolidated.md` v3.2 (123 BL disposition + canonicality + priority)
- `development-records/plan/20260413-m03-decisions.md` v1.3 (DR-M03-01~04)
- `development-records/plan/20260413-m04-decisions.md` v1.1 (DR-M04-01/02 with anchor-reject trigger + legacy 호환 상한 + Stage 3 monitor)
- `development-records/plan/20260413-deferred-ledger.md` v1.6 (M-04-A 완료 + DL-031 신설)
- `authority/core-lexicon.yaml` v0.6.1 (activity_enum + axis_enum canonical seat + retire_kind 구조)

## 6. 개정 이력

- v1 (2026-04-13T15:30): 파일 신설. schema + 축별 빈 섹션 확정. item_count=0. M-06 이후 채움.
- v1.1 (2026-04-13T16:10): 9-lens review session `20260413-a9e93dd7` CONDITIONAL 해소 (BLOCKING 1 + MAJOR 4 + MODERATE 5). 주요 변경:
  - **C-01**: `lifecycle_status_enum_ref` 를 self-ref (§1.2) 로 교체 — broken anchor (`#M-04-field-schema`) 해소
  - **C-03**: §1.5 에 ID 공간 99 상한 명시 + 선제 regex 확장 옵션 + archived nn 재사용 금지
  - **C-04**: §1.3 에 `depends_on` 의미 구분 (W=실행 DAG, BL=resolves, DL=resolution_stage 도달) + provenance vs depends_on 분리
  - **C-05**: §1.1 row 3 `files` glob 허용 명시 + §1.3 cluster 판정 알고리즘 + ownership_boundary↔files prefix 불변식
  - **C-08**: §1.1 row 17 `change_type` enum 명시 + rewrite_trace 의미 통합 (단일 log)
  - **CC-03**: frontmatter `source_refs` 에 `core_lexicon`, `m04_decisions` 추가
  - **G-2**: §1.1 row 12 `lifecycle_status` 초기값 `active` 명시
  - **C-10/G-3**: §1.5 schema_version 표기법 (`v{major}.{minor}`) 명시
  - **C-02**: §4 Phase B 재호출 contract — Exit 경로 (PASS/FAIL/FAIL-boundary) + 추적 경로 + ledger DL-031 연계
  - frontmatter `lexicon_version_at_creation`, enum_ref 모두 v0.6.1 로 갱신.
  - frontmatter `m04_decisions` source_ref 연계 (m04-decisions.md v1.1).
  - `review_session_ref` 추가.
- v1.2 (2026-04-13T16:25): codex review session `20260413-de95c971` PASS-with-residuals 후속 정정 (Immediate 1/2/4 + Recommendation 2/3). 주요 변경:
  - **Immediate 1 (UF-logic-L-01)**: deferred-ledger DL-024 의 "13필드" → "17필드" + 잔재 표기 주석.
  - **Immediate 2 (C-02)**: §2 W-A 의존 문장 명료화 — "A0 자체는 axis=B (activity=reconstruct), 모든 W-A-* 는 A0 의 W-B-NN 을 depends_on" + §2 W-B 에 axis arrow 의미 ("A→B = B 는 A 의 선행") + A0 위치 ownership 명시.
  - **Immediate 4 (C-01)**: §1.2 enum snapshot v0.6.0 → v0.6.1 + §5 references v1/v0.6.0 → v1.1/v0.6.1.
  - **Recommendation 2 (UF-semantics-MI-S-1)**: execution-log `phase_b_status: "skipped"` → `deferred-to-M05-entry` (ledger DL-031 추적 contract 와 wording 일치).
  - **Recommendation 3 (UF-evolution-E-15)**: execution-log frontmatter `as_of` 16:25 갱신 + review_sessions multi-entry 구조.
  - 본 review session ref 추가 (de95c971, codex executor).
